apiVersion: v1
kind: Service
metadata:
  name: genaiops-core-processing-backend
  labels: { app: genaiops-core-processing-backend }
spec:
  type: ClusterIP
  selector: { app: genaiops-core-processing-backend }
  ports: [ { name: http, port: 8083, targetPort: http } ]
